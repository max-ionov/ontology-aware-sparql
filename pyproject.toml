[project]
name = "ontology-aware-sparql"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain",
    "langchain-ollama",
    "langchain-huggingface",
    "chainlit",
]

[dependency-groups]
dev = [
    "jupyter",
    "ipykernel"
]